name: file list
  
on:
  schedule:
    - cron: '0 9 * * *'
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout code
        uses: actions/checkout@v3

      - name: echo files
        run: echo $(find . -type f) 
